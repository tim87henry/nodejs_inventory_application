<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= title %></title>
    <%- include('head') -%>
</head>
<body>
    <% if (tvshows.length === 0) { %>
        <h2>Do you want to delete <%= network.name %>?</h2>
        <form action="" method="POST">
            <button id="cancel" name="cancel" value="cancel" class="btn btn-primary">Cancel</button>
            <button id="delete" name="delete" value="delete" class="btn btn-primary">Delete</button>
        </form>
    <% } else { %>
        <h2>The following TV shows have this network. Please delete them first.</h2>
        <ul class="appList">
            <% tvshows.forEach( tvshow => { %>
                <li><div>
                    <h3><a href=<%= tvshow.url %>><%= tvshow.name %></a></h3>
                    <p><%= tvshow.desc %></p>
                </div></li>
            <% }) %>
        </ul>
        <a href=<%= network.url %>><button class="btn btn-primary"><</button></a>
    <% } %>
</body>
</html>